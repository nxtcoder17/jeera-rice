#include <bits/stdc++.h>
using namespace std;

#define d_prefix(prefix) cerr << "[" << #prefix << "]: "
#define debug(item) d_prefix(item); cerr << item << endl;
#define debugf(msg, item) cerr << "(" << msg << ") " ; debug(item);
#define debugi(arr) d_prefix(arr); for(auto item: arr) { cerr << " " << item << "," ;} cerr << endl;

int main() {
}
