sudo mount -o bind /mnt/data-disk/workspace $HOME/workspace
sudo mount -o bind /mnt/data-disk/.local/share/node $HOME/.local/share/node
sudo mount -o bind /mnt/data-disk/.local/share/go $HOME/.local/share/go
